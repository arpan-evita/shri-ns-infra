/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'standalone',
  images: {
    domains: ['images.unsplash.com', 'bpdvcyceybngwrbfnubr.supabase.co'],
  },
};

export default nextConfig;
